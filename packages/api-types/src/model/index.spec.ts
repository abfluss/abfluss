/*!
 * Source https://github.com/abfluss/abfluss Package: api-types
 */

import { expect } from 'chai';
import 'mocha';

describe('model/index', (): void => {
    it('should contain all', (): void => {
        // tslint:disable-next-line:no-unused-expression
        expect(true).to.be.true;
    });
});
